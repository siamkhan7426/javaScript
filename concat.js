// Array concat --> array vitor value ak sata korba

const letters = ["a", "b", "c"];
const numbers = [1, 2, 3];
const age = [18, 20, 30];
const alpaNumber = letters.concat(numbers, age) // value  concat
console.log(alpaNumber)

// output 'a', 'b', 'c', 1,  2, 3,   18,  20,  30